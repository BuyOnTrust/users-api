{
  "name": "optin-api",
  "version": "1.0.0",
  "description": "An API to handle RESTful CRUD operations for user optin and data gathering.",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "sls offline start --skipCacheInvalidation"
  },
  "author": "Weston Gibler",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^5.12.0"
  },
  "dependencies": {
    "dotenv": "^8.1.0",
    "mongoose": "^5.7.4"
  }
}
